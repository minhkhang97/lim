// import { IOrderDetail } from "./interfaces/order.dto"

// export const genName = (orderDetail: any) => {
//     let attributeName = '';
//     orderDetail.attributes.forEach((attr: any) => {
//         attributeName += `[${attr.name} ${attr.options[0].name}]`;
//     });
//     const productName = `${attributeName}${orderDetail.product.name} x ${orderDetail.amount}`;
//     return productName;
// }